# Database connection string (from docker-compose.yml)
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/yourown?schema=public"

# Secret for JWT signing
JWT_SECRET="dev-secret-change-me"

# Optional: Directory for export templates
# RENDERER_TEMPLATE_DIR="./renderer"

# Node environment
NODE_ENV="development"
