// App constants

export const GRID_COLUMNS = 4;
export const MIN_CARD_SIZE = 1;
export const MAX_CARD_SIZE = 4;

export const ANIMATION_DURATIONS = {
  FAST: 0.3,
  NORMAL: 0.5,
  SLOW: 0.6,
} as const;

export const BLUR_AMOUNTS = {
  LIGHT: 4,
  MEDIUM: 8,
  HEAVY: 12,
} as const;

export const EXPORT_FORMATS = {
  PDF: 'pdf',
  IMAGE: 'image',
} as const;

